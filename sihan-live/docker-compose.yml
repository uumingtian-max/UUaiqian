version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - OPENAI_BASE_URL=http://host.docker.internal:8000/v1
      - MODEL_ID=Qwen3.6-35B-A3B-Uncensored-HauhauCS-Aggressive-Q8_K_P.gguf
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8001:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
